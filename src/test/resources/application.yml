spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  sql:
    init:
      mode: never

livekit:
  url: ws://localhost
  api-key: test
  api-secret: test
  agent-name: interviewer-agent

jwt:
  secret: test-secret-key-for-context-loads-test-secret-key
  expiration: 3600000

llm:
  provider: mock

aws:
  region: us-east-1

evaluation:
  dispatcher: direct
  queue:
    turn-url:
    turn-name: pj-kmucd1-10-turn-evaluation
    session-url:
    session-name: pj-kmucd1-10-session-evaluation

cors:
  allowed-origins: http://localhost:5173
